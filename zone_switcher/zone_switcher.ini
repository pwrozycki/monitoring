[db]
host = localhost
user = zmuser
passwd = zmpass
database = zm

[thresholds]
query = update Zones set MinPixelThreshold = %(threshold)s
        where Type = 'Active'
        and MinPixelThreshold != %(threshold)s
night_threshold = 12
day_threshold = 17
astral_city = Warsaw
